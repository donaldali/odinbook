- user    = User.find(@receiver_id)
- friends = user.friends

%ul.users-container
  %li.friends-header
    %h2
      = "#{user.first_name}'s friends"
      %span= "#{friends.count}"

  = render friends, is_index: true
