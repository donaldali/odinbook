- if current_user == commenter
  = link_to comment_path(comment), method: :delete, class: "delete",
                                   remote: true do
    = image_tag("close.png", title: "Delete Comment")
